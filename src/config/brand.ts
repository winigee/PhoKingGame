/**
 * Brand configuration. The game/brand name must never be hard-coded
 * elsewhere; import BRAND_NAME from here.
 */
export const BRAND_NAME = 'Pho King';
export const GAME_TITLE = `${BRAND_NAME} Life`;
